# {TSK-ID}: TDD 구현 결과

## 결과: PASS / FAIL

## 생성/수정된 파일

E2E 테스트 파일도 이 표에 포함한다 (아래 E2E 섹션은 검증 대상 매핑용). E2E 테스트는 build에서 실행하지 않으므로 신규/수정 칸에 `신규 (build 작성, 실행은 dev-test)`로 표기한다.

| 파일 경로 | 변경 내용 | 신규/수정 |
|-----------|-----------|-----------|
| path/to/file | 설명 | 신규 |

## 테스트 결과

| 구분 | 통과 | 실패 | 합계 |
|------|------|------|------|
| 단위 테스트 | - | - | - |

## E2E 테스트 (작성만 — 실행은 dev-test)

| 파일 경로 | 검증 대상 |
|-----------|-----------|
| (E2E 테스트 파일 경로) | (QA 체크리스트 항목) |

- `{DOMAIN}`이 `fullstack`/`frontend`가 아닌 경우 (backend/default 등): 단일 행으로 `N/A — {domain} domain`을 기록한다.
- `{DOMAIN}`은 UI지만 Dev Config에 `e2e_test`가 정의되지 않은 경우: `N/A — e2e_test 미정의` 기록.

## 커버리지 (Dev Config에 coverage 정의 시)
- 커버리지: (퍼센트 또는 요약)
- 미커버 파일: (있으면 나열, 없으면 "없음")
- 미정의 시 "N/A" 기록

## 비고
- (특이사항, 없으면 생략)
